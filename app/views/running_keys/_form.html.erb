<%= form_for(@running_key) do |f| %>
  <% if @running_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@running_key.errors.count, "error") %> prohibited this running_key from being saved:</h2>

      <ul>
      <% @running_key.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.hidden_field :book_id, value:@book.id %>
  <%= f.hidden_field :user_id, value:@user.id %>
  <div class="field">
    <%= f.label :input %><br>
    <%= f.text_area :input %>
  </div>
  <div class="field">
    <%= f.label :page_number %><br>
    <%= f.select :page_number, options_for_select(@book.pages.pluck(:page_number)) %>
  </div> 
  <div class="field">
    <%= f.label :line %><br>
    <%= f.number_field :line %>
  <div class="field">
    <%= f.label :group_length %><br>
    <%= f.number_field :group_length %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
